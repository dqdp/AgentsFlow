version: 1
contract: task.contract.md
bindings:
  - id: AF-BHV-001
    scenario: "Required behavior scenario name"
    required: true
    status: executable
    risk_surfaces:
      - authority_boundary
    path_class: valid_delegation
    evidence_class: script
    failure_path_matrix_refs:
      - FPM-001
    source:
      type: contract
      path: task.contract.md
      section: Behavioral Scenarios
    severity_if_unverified: P1
    checks:
      - id: example-check
        type: script
        command: "python3 scripts/example_check.py"
        evidence:
          - example-check-report
    gates:
      - verification_gate
